import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { Navigation } from "@/components/navigation";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Email Extractor - Financial Transaction Parser",
  description: "Extract and consolidate financial transaction data from email notifications",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased sidebar-expanded`}
      >
        <Navigation />
        <div className="main-content min-h-screen bg-gray-50 transition-all duration-300">
          {children}
        </div>
        <Toaster />
      </body>
    </html>
  );
}
